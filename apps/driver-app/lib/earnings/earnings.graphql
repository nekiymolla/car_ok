query GetStats {
    getStats(timeframe: Daily) {
        currency
        dataset {
            count
            current
            distance
            earning
            name
            time
        }
    }
}